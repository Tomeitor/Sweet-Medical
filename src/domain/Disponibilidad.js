export default class Disponibilidad {
    constructor({id, diaSemana, desde, hasta}) {
        this.id = id;
        this.diaSemana = diaSemana;
        this.desde = desde;
        this.hasta = hasta;
    }
}