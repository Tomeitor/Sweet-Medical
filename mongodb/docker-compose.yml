services:
  mongodb:
    image: mongo:7
    container_name: backend-grupo-02-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: backend-grupo-02
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
